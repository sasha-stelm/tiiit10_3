# 📧 Email Extractor C++

![C++](img.shields.io)
![License](img.shields.io)

Небольшой, но эффективный инструмент на языке **C++**, предназначенный для автоматического поиска и извлечения адресов электронной почты из неструктурированного текста.

## 🚀 Основные возможности
- **Regex Engine**: Использует мощную библиотеку `<regex>` для точного сопоставления шаблонов.
- **Validation**: Проверяет структуру email (имя пользователя, домен, доменную зону).
- **Iteration**: Обрабатывает весь текст целиком с помощью итераторов и выводит список найденных совпадений.

## 🛠 Технологический стек
- **Язык**: C++ (стандарт C++11 и выше)
- **Библиотеки**: `iostream`, `string`, `regex`

## 📦 Как запустить

### 1. Требования
Убедитесь, что у вас установлен компилятор (GCC, Clang или MSVC).

### 2. Сборка проекта
Скомпилируйте исходный код через командную строку:
```bash
g++ emails.cpp -o email_finder
